# Copy to .env and fill in. Restart Vite after changing.

# OpenRouter API key (https://openrouter.ai/keys)
# Without this key, MoiezBot uses a local keyword fallback so the UI still works.
VITE_OPENROUTER_API_KEY=

# Optional — defaults to openrouter/free (auto-picks an available free model)
# VITE_OPENROUTER_MODEL=openrouter/free
# Or pin a free slug, e.g. google/gemma-4-31b-it:free / openai/gpt-oss-20b:free
